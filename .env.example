# Application Configuration
# Copy this file to .env and update with your values
#
# NOTE: When using Docker Compose, DATABASE_URL and REDIS_HOST are automatically
# overridden to use Docker service names (postgres, redis) instead of localhost.

# PostgreSQL Configuration (for Docker Compose)
POSTGRES_USER=tasky
POSTGRES_PASSWORD=tasky
POSTGRES_DB=tasky

# Backend Configuration
DATABASE_URL="postgresql://tasky:tasky@localhost:5432/tasky"

# Application
# Use 'development' for local dev, 'production' for Docker Compose
NODE_ENV=development

# # Backend Server Port (default: 3000)
HOST=0.0.0.0

# Authentication & Security
# Generate secure values using:
#   openssl rand -base64 32  (for JWT secrets)
#   openssl rand -hex 32     (for encryption key)
JWT_SECRET="jq2Npnpp0pl9tuKowA1opDBXtjgsrxxOm87rNEgFv5SYAnzF"
JWT_REFRESH_SECRET="evbRTnQhWPqpgZSoVA4uQXKuFjw0UdtmYrvJaGdj6xlxbZp9"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Encryption for sensitive data (email passwords, etc.)
ENCRYPTION_KEY="Nj9vBnl9xBzZFwPAHe2FTq0x7H1cNeAuuYkqu45r4kP4DAoR"

# Redis Configuration (for Bull Queue)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Email Configuration (for notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@tasky.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@tasky.com
EMAIL_DOMAIN="tasky.com"

FRONTEND_URL=http://localhost:3001
CORS_ORIGIN="http://localhost:3001"

# Backend API URL (for frontend to connect to backend)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# File Upload
UPLOAD_DEST="./uploads"
MAX_FILE_SIZE=10485760

# Editor Image Upload Configuration
MAX_EDITOR_IMAGE_SIZE=5242880
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/gif,image/webp
EDITOR_IMAGE_UPLOAD_FOLDER=editor-images

# Queue Configuration
MAX_CONCURRENT_JOBS=5
JOB_RETRY_ATTEMPTS=3
# Bull Board basic auth (leave both empty to disable auth)
BULL_BOARD_EMAIL="admin@tasky.com"
BULL_BOARD_PASSWORD="password123"

# Jira Integration
# Comma-separated list of allowed Jira hostnames (SSRF allowlist).
# Supports exact hostnames (jira.mycompany.com) and single-level wildcards (*.atlassian.net).
# Use * to allow any hostname (not recommended in production).
# Defaults to *.atlassian.net when unset or blank.
# JIRA_ALLOWED_HOSTS=*.atlassian.net

# AI Provider Endpoint
# The AI endpoint is read from settings any signed-in user can write, so the
# server validates it before sending anything, the same way it validates Jira.
# Comma-separated allowlist of hostnames the AI endpoint may point at.
# Supports exact hostnames and single-level wildcards (*.openai.com).
# Defaults to * (any public host) when unset or blank.
# AI_ALLOWED_HOSTS=api.openai.com,*.anthropic.com
#
# Permit AI endpoints on the server's own network (localhost, RFC 1918).
# Needed for a self-hosted model server such as Ollama. Off by default: with it
# on, anyone who can edit AI settings can make the server call anything it can
# reach, so turn it on only alongside a narrow AI_ALLOWED_HOSTS.
# AI_ALLOW_PRIVATE_ENDPOINTS=false

# AWS S3 Configuration (optional, for cloud storage)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1
AWS_BUCKET_NAME=your-bucket-name
